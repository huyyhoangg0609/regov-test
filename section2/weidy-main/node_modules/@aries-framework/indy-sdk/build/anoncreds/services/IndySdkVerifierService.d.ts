import type { AnonCredsVerifierService, VerifyProofOptions } from '@aries-framework/anoncreds';
import { IndySdk } from '../../types';
export declare class IndySdkVerifierService implements AnonCredsVerifierService {
    private indySdk;
    constructor(indySdk: IndySdk);
    verifyProof(options: VerifyProofOptions): Promise<boolean>;
}
