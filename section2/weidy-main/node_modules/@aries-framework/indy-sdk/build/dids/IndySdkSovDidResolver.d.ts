import type { DidResolutionResult, ParsedDid, DidResolver, AgentContext } from '@aries-framework/core';
import { IndySdkPoolService } from '../ledger/IndySdkPoolService';
import { IndySdk } from '../types';
export declare class IndySdkSovDidResolver implements DidResolver {
    private indySdk;
    private indySdkPoolService;
    constructor(indyPoolService: IndySdkPoolService, indySdk: IndySdk);
    readonly supportedMethods: string[];
    resolve(agentContext: AgentContext, did: string, parsed: ParsedDid): Promise<DidResolutionResult>;
    private getPublicDid;
    private getEndpointsForDid;
}
